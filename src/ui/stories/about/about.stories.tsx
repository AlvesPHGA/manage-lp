import { Meta, StoryObj } from '@storybook/react';
import { About } from '../../partials/home/About';

export default {
   title: 'Partials/About',
   component: About,
} as Meta;

export const AboutComponent: StoryObj = {};
