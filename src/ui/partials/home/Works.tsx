import { WorksContent } from '../../components/works/worksContent';

export const Works = () => {
   return (
      <section className="relative border py-20 bg-brightRed bg-[url('./icons/bg-simplify-section-desktop.svg')] bg-no-repeat bg-rb-screen-1200plus bg-bs-1200plus">
         <WorksContent />
      </section>
   );
};
