import { Meta, StoryObj } from '@storybook/react';
import { Works } from '../../partials/home/Works';

export default {
   title: 'Partials/Works',
   component: Works,
} as Meta;

export const WorksPartials: StoryObj = {};
