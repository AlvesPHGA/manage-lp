import { Meta, StoryObj } from '@storybook/react';
import { Intro } from '../../partials/home/Intro';

export default {
   title: 'Partials/Intro',
   component: Intro,
} as Meta;

export const Default: StoryObj = {};
