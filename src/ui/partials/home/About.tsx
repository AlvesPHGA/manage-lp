import { AboutContent } from '../../components/about/AboutContent';

export const About = () => {
   return (
      <section>
         <div>
            <AboutContent />
         </div>
      </section>
   );
};
