import { WorksContent } from '../../components/works/worksContent';

export const Works = () => {
   return (
      <section className="py-20 bg-brightRed">
         <WorksContent />
      </section>
   );
};
